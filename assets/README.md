# assets

이 폴더는 로컬 폴백 이미지·아이콘·폰트(선택)를 두는 곳입니다.
현재 빌드는 외부 폰트(google_fonts)와 TourAPI 원격 이미지를 사용하므로
필수 에셋은 없습니다. pubspec.yaml 의 `assets:` 항목이 이 폴더를 가리킵니다.

권장:
- 오프라인 모드용 축제 대표 이미지 캐시
- 앱 아이콘 / 스플래시 로고 (flutter_launcher_icons 사용 시)
